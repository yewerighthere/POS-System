<Window x:Class="SmartPOS.WPF.Views.CreatePromotionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        mc:Ignorable="d"
        Title="Create Promotion" Height="750" Width="1100" WindowStartupLocation="CenterScreen"
        Background="#F8FAFC">

    <Grid>
        

        <!-- Left Pane (Dark) -->
        

        <!-- Right Pane (Light Form Area) -->
        <Grid Background="#F8FAFC">
            <!-- Watermarks -->
            <TextBlock Text="v1.0.0" Foreground="#CBD5E1" FontSize="12" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="30,0,0,30"/>
            <TextBlock Text="SmartPOS Retail" Foreground="#CBD5E1" FontSize="12" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,30,30"/>

            <!-- Form Card -->
            <Border Background="White" CornerRadius="12" Margin="40,20,40,20" Padding="40" VerticalAlignment="Center" MaxWidth="550">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.04" BlurRadius="25" ShadowDepth="4" Direction="270"/>
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Create Promotion" FontSize="26" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,5"/>
                    <TextBlock Grid.Row="1" Text="Fill in the details for the new promotion campaign" FontSize="14" Foreground="#64748B" Margin="0,0,0,25"/>

                    <!-- Row 1: Code & Name -->
                    <Grid Grid.Row="2" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Code" FontSize="12" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,6"/>
                            <TextBox Text="{Binding Code, UpdateSourceTrigger=PropertyChanged}" Padding="8,6"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Name" FontSize="12" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,6"/>
                            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Padding="8,6"/>
                        </StackPanel>
                    </Grid>

                    <!-- Row 2: Type & Discount -->
                    <Grid Grid.Row="3" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Type" FontSize="12" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,6"/>
                            <ComboBox SelectedValue="{Binding Type}" SelectedValuePath="Content" HorizontalAlignment="Stretch" Padding="8,6">
                                <ComboBoxItem Content="Percentage"/>
                                <ComboBoxItem Content="FixedAmount"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Discount Value" FontSize="12" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,6"/>
                            <TextBox Text="{Binding DiscountValue, UpdateSourceTrigger=PropertyChanged}" Padding="8,6"/>
                        </StackPanel>
                    </Grid>

                    <!-- Row 3: Dates -->
                    <Grid Grid.Row="4" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Start Date" FontSize="12" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,6"/>
                            <DatePicker SelectedDate="{Binding StartDate}" HorizontalAlignment="Stretch" Padding="4"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="End Date" FontSize="12" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,6"/>
                            <DatePicker SelectedDate="{Binding EndDate}" HorizontalAlignment="Stretch" Padding="4"/>
                        </StackPanel>
                    </Grid>

                    <!-- Row 4: Min Order & Active -->
                    <Grid Grid.Row="5" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Min Order Amount (Optional)" FontSize="12" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,6"/>
                            <TextBox Text="{Binding MinOrderAmount, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}" Padding="8,6"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,20,0,0">
                            <CheckBox Content="Enable this promotion" IsChecked="{Binding IsActive}" Foreground="#334155" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Grid>

                    <!-- Description -->
                    <StackPanel Grid.Row="6" Margin="0,0,0,25">
                        <TextBlock Text="Description" FontSize="12" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,6"/>
                        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" Height="60" Padding="8,6"/>
                    </StackPanel>

                    <!-- Error message -->
                    <TextBlock Grid.Row="7" Text="{Binding ErrorMessage}" Foreground="#EF4444" FontSize="13" TextWrapping="Wrap" Margin="0,0,0,15"/>

                    <!-- Action Buttons -->
                    <Button Grid.Row="8" Command="{Binding SaveCommand}" Background="#022B94" Foreground="White" BorderThickness="0" HorizontalAlignment="Stretch" Height="42" Cursor="Hand" Margin="0,0,0,15">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="Create Promotion" FontWeight="SemiBold" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="→" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Grid.Row="9" Content="Cancel" Command="{Binding CancelCommand}" Background="Transparent" Foreground="#64748B" BorderThickness="0" HorizontalAlignment="Center" Cursor="Hand" Padding="20,5"/>

                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
