<UserControl x:Class="SmartPOS.WPF.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="32">
        <StackPanel Width="320" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="SmartPOS" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,24" />
            <TextBlock Text="Tên đăng nhập" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
            <TextBlock Text="Mật khẩu" />
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,16" />
            <Button Content="Đăng nhập" Command="{Binding LoginCommand}" Height="36" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Crimson" TextWrapping="Wrap" Margin="0,12,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
