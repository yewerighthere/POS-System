<UserControl x:Class="SmartPOS.WPF.Views.InvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:SmartPOS.WPF.ViewModels"
             xmlns:converters="clr-namespace:SmartPOS.WPF.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:InvoiceViewModel}"
             Background="{StaticResource BackgroundBrush}"
             d:DesignHeight="720" d:DesignWidth="960">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:NullOrEmptyToVisibilityConverter x:Key="NullToVis"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Border Background="White" CornerRadius="12" Padding="24" MaxWidth="760" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="28" ShadowDepth="4" Opacity="0.12" Color="#000000"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <TextBlock Text="Hóa đơn" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Text="{Binding InvoiceStatus}" FontSize="13" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
                </StackPanel>

                <Border Grid.Row="1" Margin="0,16,0,16" Background="{StaticResource BackgroundBrush}" BorderBrush="{StaticResource BorderMediumBrush}" BorderThickness="1" CornerRadius="10" Padding="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,16,0">
                            <TextBlock Text="Số hóa đơn" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
                            <TextBlock Text="{Binding Invoice.InvoiceNumber}"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       FontSize="15"
                                       FontWeight="SemiBold"
                                       TextWrapping="NoWrap"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                            <TextBlock Text="Tổng tiền" Foreground="{StaticResource TextMutedBrush}" FontSize="12" HorizontalAlignment="Right"/>
                            <TextBlock Foreground="{StaticResource TextPrimaryBrush}" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Right">
                                <Run Text="{Binding Invoice.TotalAmount, StringFormat={}{0:N0}}"/>
                                <Run Text=" đ"/>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Row="2" Background="{StaticResource BackgroundBrush}" BorderBrush="{StaticResource BorderMediumBrush}" BorderThickness="1" CornerRadius="10" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Thông tin hóa đơn" FontSize="13" FontWeight="SemiBold" Foreground="#334155"/>
                        <TextBlock Margin="0,10,0,0" Foreground="#475569">
                            <Run Text="Mã đơn: "/>
                            <Run Text="{Binding Invoice.OrderId}"/>
                        </TextBlock>
                        <TextBlock Margin="0,6,0,0" Foreground="#475569">
                            <Run Text="Ngày phát hành: "/>
                            <Run Text="{Binding IssuedAtLocal, Mode=OneWay, StringFormat={}{0:dd/MM/yyyy HH:mm:ss}}"/>
                        </TextBlock>
                        <TextBlock Margin="0,6,0,0" Foreground="#475569" Visibility="{Binding Invoice.CustomerName, Converter={StaticResource NullToVis}, FallbackValue=Collapsed}">
                            <Run Text="Khách hàng: "/>
                            <Run Text="{Binding Invoice.CustomerName}"/>
                        </TextBlock>
                        <TextBlock Margin="0,6,0,0" Foreground="#16A34A" Visibility="{Binding Invoice.CustomerName, Converter={StaticResource NullToVis}, FallbackValue=Collapsed}">
                            <Run Text="Sử dụng: -"/>
                            <Run Text="{Binding Invoice.PointsUsed, StringFormat={}{0:N0}}"/>
                            <Run Text=" điểm | Tích lũy: +"/>
                            <Run Text="{Binding Invoice.PointsEarned, StringFormat={}{0:N0}}"/>
                            <Run Text=" điểm | Số dư: "/>
                            <Run Text="{Binding Invoice.CurrentPointsBalance, StringFormat={}{0:N0}}"/>
                            <Run Text=" điểm"/>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button Content="Xem trước / In giả lập"
                            Command="{Binding PreviewCommand}"
                            Height="42"
                            MinWidth="160"
                            Padding="16,0"
                            Background="{StaticResource InfoBrush}"
                            Foreground="White"
                            BorderThickness="0"
                            Margin="0,0,10,0"/>
                    <Button Content="Đóng"
                            Height="42"
                            MinWidth="96"
                            Padding="16,0"
                            Background="{StaticResource BorderMediumBrush}"
                            Foreground="{StaticResource TextPrimaryBrush}"
                            BorderThickness="0"
                            Command="{Binding CloseCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Background="{StaticResource OverlayBrush}" Visibility="{Binding IsPreviewVisible, Converter={StaticResource BoolToVis}}">
            <Border Width="420"
                    Background="White"
                    CornerRadius="14"
                    Padding="24"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="32" ShadowDepth="6" Opacity="0.2" Color="#000000"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel>
                        <TextBlock Text="Xem trước hóa đơn"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="In giả lập thành công"
                                   FontSize="13"
                                   Foreground="{StaticResource InfoBrush}"
                                   Margin="0,4,0,14"/>
                    </StackPanel>
                    <Border Grid.Row="1"
                            Background="{StaticResource BackgroundBrush}"
                            BorderBrush="{StaticResource BorderMediumBrush}"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="16">
                        <ScrollViewer MaxHeight="360" VerticalScrollBarVisibility="Auto">
                            <TextBlock Text="{Binding ReceiptPreview}"
                                       FontFamily="Consolas"
                                       FontSize="13"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       TextWrapping="Wrap"/>
                        </ScrollViewer>
                    </Border>
                    <Button Grid.Row="2"
                            Content="Đóng xem trước"
                            Command="{Binding ClosePreviewCommand}"
                            Height="42"
                            MinWidth="140"
                            HorizontalAlignment="Right"
                            Margin="0,16,0,0"
                            Background="{StaticResource InfoBrush}"
                            Foreground="White"
                            BorderThickness="0"/>
                </Grid>
            </Border>
        </Border>

        <Border Background="{StaticResource OverlayBrush}" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />
    </Grid>
</UserControl>
