<UserControl x:Class="SmartPOS.WPF.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartPOS.WPF.Views"
             xmlns:viewmodels="clr-namespace:SmartPOS.WPF.ViewModels"
             xmlns:converters="clr-namespace:SmartPOS.WPF.Converters"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:SalesViewModel}"
             Background="#F8FAFC"
             Foreground="#0F172A"
             d:DesignHeight="768" d:DesignWidth="1024">
             
     <UserControl.Resources>
         <converters:NullOrEmptyToVisibilityConverter x:Key="NullOrEmptyToVisibilityConverter" />
         <converters:DecimalToVisibilityConverter x:Key="DecimalToVisibilityConverter" />
         <converters:ProductImageConverter x:Key="ProductImageConverter" />
         <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
         
         <!-- Modern ScrollViewer style for clean UI -->
         <Style x:Key="ModernScrollViewer" TargetType="{x:Type ScrollViewer}">
             <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
             <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="{x:Type ScrollViewer}">
                         <Grid>
                             <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                             <ScrollBar x:Name="PART_VerticalScrollBar" 
                                        Value="{TemplateBinding VerticalOffset}" 
                                        Maximum="{TemplateBinding ScrollableHeight}" 
                                        ViewportSize="{TemplateBinding ViewportHeight}" 
                                        Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" 
                                        HorizontalAlignment="Right" 
                                        Width="8"/>
                         </Grid>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
         </Style>

         <!-- Sidebar Inactive Button Style -->
         <Style x:Key="SidebarInactiveButton" TargetType="Button">
             <Setter Property="Background" Value="Transparent"/>
             <Setter Property="Foreground" Value="#94A3B8"/>
             <Setter Property="BorderThickness" Value="0"/>
             <Setter Property="Padding" Value="16,10"/>
             <Setter Property="Margin" Value="8,2"/>
             <Setter Property="Cursor" Value="Hand"/>
             <Setter Property="HorizontalContentAlignment" Value="Left"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="Button">
                         <Border Name="Border" CornerRadius="8" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                             <StackPanel Orientation="Horizontal">
                                 <TextBlock Text="{TemplateBinding Tag}" Margin="0,0,12,0" FontSize="14" VerticalAlignment="Center"/>
                                 <ContentPresenter VerticalAlignment="Center"/>
                             </StackPanel>
                         </Border>
                         <ControlTemplate.Triggers>
                             <Trigger Property="IsMouseOver" Value="True">
                                 <Setter TargetName="Border" Property="Background" Value="#2A2B35"/>
                                 <Setter Property="Foreground" Value="White"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
         </Style>

         <!-- New Transaction Button Style -->
         <Style x:Key="NewTransactionButton" TargetType="Button">
             <Setter Property="Background" Value="#1E40AF"/>
             <Setter Property="Foreground" Value="White"/>
             <Setter Property="FontWeight" Value="SemiBold"/>
             <Setter Property="BorderThickness" Value="0"/>
             <Setter Property="Height" Value="40"/>
             <Setter Property="Cursor" Value="Hand"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="Button">
                         <Border Name="Border" CornerRadius="8" Background="{TemplateBinding Background}">
                             <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                         </Border>
                         <ControlTemplate.Triggers>
                             <Trigger Property="IsMouseOver" Value="True">
                                 <Setter TargetName="Border" Property="Background" Value="#1D4ED8"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
         </Style>

         <!-- End Shift Button Style -->
         <Style x:Key="EndShiftButton" TargetType="Button">
             <Setter Property="Background" Value="#DC2626"/>
             <Setter Property="Foreground" Value="White"/>
             <Setter Property="FontWeight" Value="Bold"/>
             <Setter Property="BorderThickness" Value="0"/>
             <Setter Property="Padding" Value="16,8"/>
             <Setter Property="Height" Value="36"/>
             <Setter Property="Cursor" Value="Hand"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="Button">
                         <Border Name="Border" CornerRadius="8" Background="{TemplateBinding Background}">
                             <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="12,0"/>
                         </Border>
                         <ControlTemplate.Triggers>
                             <Trigger Property="IsMouseOver" Value="True">
                                 <Setter TargetName="Border" Property="Background" Value="#B91C1C"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
         </Style>

         <!-- Search Button Style -->
         <Style x:Key="SearchButton" TargetType="Button">
             <Setter Property="Background" Value="#00288E"/>
             <Setter Property="Foreground" Value="White"/>
             <Setter Property="FontWeight" Value="SemiBold"/>
             <Setter Property="BorderThickness" Value="0"/>
             <Setter Property="Height" Value="42"/>
             <Setter Property="Cursor" Value="Hand"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="Button">
                         <Border Name="Border" CornerRadius="8" Background="{TemplateBinding Background}">
                             <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                         </Border>
                         <ControlTemplate.Triggers>
                             <Trigger Property="IsMouseOver" Value="True">
                                 <Setter TargetName="Border" Property="Background" Value="#1E40AF"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
         </Style>

         <!-- Category Filter Button Style -->
         <Style x:Key="CategoryFilterButton" TargetType="RadioButton">
             <Setter Property="Background" Value="White"/>
             <Setter Property="Foreground" Value="#64748B"/>
             <Setter Property="BorderBrush" Value="#E2E8F0"/>
             <Setter Property="BorderThickness" Value="1"/>
             <Setter Property="Padding" Value="18,8"/>
             <Setter Property="Margin" Value="0,0,8,0"/>
             <Setter Property="Height" Value="36"/>
             <Setter Property="Cursor" Value="Hand"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="RadioButton">
                         <Border Name="Border" CornerRadius="18" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                             <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                         </Border>
                         <ControlTemplate.Triggers>
                             <Trigger Property="IsChecked" Value="True">
                                 <Setter TargetName="Border" Property="Background" Value="#00288E"/>
                                 <Setter TargetName="Border" Property="BorderThickness" Value="0"/>
                                 <Setter Property="Foreground" Value="White"/>
                             </Trigger>
                             <Trigger Property="IsMouseOver" Value="True">
                                 <Setter TargetName="Border" Property="BorderBrush" Value="#00288E"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
         </Style>

         <!-- Add Product Button Style -->
         <Style x:Key="AddProductButton" TargetType="Button">
             <Setter Property="Background" Value="#DBEAFE"/>
             <Setter Property="Foreground" Value="#00288E"/>
             <Setter Property="FontWeight" Value="Bold"/>
             <Setter Property="FontSize" Value="16"/>
             <Setter Property="Width" Value="36"/>
             <Setter Property="Height" Value="36"/>
             <Setter Property="BorderThickness" Value="0"/>
             <Setter Property="Cursor" Value="Hand"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="Button">
                         <Border Name="Border" CornerRadius="8" Background="{TemplateBinding Background}">
                             <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-2,0,0"/>
                         </Border>
                         <ControlTemplate.Triggers>
                             <Trigger Property="IsMouseOver" Value="True">
                                 <Setter TargetName="Border" Property="Background" Value="#00288E"/>
                                 <Setter Property="Foreground" Value="White"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
         </Style>

         <!-- Qty Adjust Button Style -->
         <Style x:Key="QtyAdjustButton" TargetType="Button">
             <Setter Property="Background" Value="#F8FAFC"/>
             <Setter Property="Foreground" Value="#64748B"/>
             <Setter Property="BorderBrush" Value="#E2E8F0"/>
             <Setter Property="BorderThickness" Value="1"/>
             <Setter Property="Width" Value="24"/>
             <Setter Property="Height" Value="24"/>
             <Setter Property="FontWeight" Value="Bold"/>
             <Setter Property="Cursor" Value="Hand"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="Button">
                         <Border Name="Border" CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                             <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-1,0,0"/>
                         </Border>
                         <ControlTemplate.Triggers>
                             <Trigger Property="IsMouseOver" Value="True">
                                 <Setter TargetName="Border" Property="BorderBrush" Value="#00288E"/>
                                 <Setter Property="Foreground" Value="#00288E"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
         </Style>

         <!-- Remove Cart Item Button Style -->
         <Style x:Key="RemoveCartItemButton" TargetType="Button">
             <Setter Property="Background" Value="Transparent"/>
             <Setter Property="Foreground" Value="#94A3B8"/>
             <Setter Property="BorderThickness" Value="0"/>
             <Setter Property="Width" Value="24"/>
             <Setter Property="Height" Value="24"/>
             <Setter Property="Cursor" Value="Hand"/>
             <Setter Property="Margin" Value="8,0,0,0"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="Button">
                         <Border Name="Border" Background="{TemplateBinding Background}">
                             <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                         </Border>
                         <ControlTemplate.Triggers>
                             <Trigger Property="IsMouseOver" Value="True">
                                 <Setter Property="Foreground" Value="#DC2626"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
         </Style>

         <!-- Promo Code TextBox Style -->
         <Style x:Key="PromoTextBox" TargetType="TextBox">
             <Setter Property="Background" Value="#F8FAFC"/>
             <Setter Property="Foreground" Value="#0F172A"/>
             <Setter Property="BorderBrush" Value="#E2E8F0"/>
             <Setter Property="BorderThickness" Value="1"/>
             <Setter Property="Padding" Value="12,8"/>
             <Setter Property="Height" Value="40"/>
             <Setter Property="VerticalContentAlignment" Value="Center"/>
             <Setter Property="CaretBrush" Value="#00288E"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="TextBox">
                         <Border Name="Border" CornerRadius="8" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                             <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                         </Border>
                         <ControlTemplate.Triggers>
                             <Trigger Property="IsFocused" Value="True">
                                 <Setter TargetName="Border" Property="BorderBrush" Value="#00288E"/>
                                 <Setter TargetName="Border" Property="Background" Value="White"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
         </Style>

         <!-- Apply Promo Button Style -->
         <Style x:Key="ApplyPromoButton" TargetType="Button">
             <Setter Property="Background" Value="#F1F5F9"/>
             <Setter Property="Foreground" Value="#00288E"/>
             <Setter Property="FontWeight" Value="Bold"/>
             <Setter Property="BorderThickness" Value="0"/>
             <Setter Property="Padding" Value="16,0"/>
             <Setter Property="Height" Value="40"/>
             <Setter Property="Cursor" Value="Hand"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="Button">
                         <Border Name="Border" CornerRadius="8" Background="{TemplateBinding Background}">
                             <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                         </Border>
                         <ControlTemplate.Triggers>
                             <Trigger Property="IsMouseOver" Value="True">
                                 <Setter TargetName="Border" Property="Background" Value="#E2E8F0"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
         </Style>

         <!-- Cancel Order Button Style -->
         <Style x:Key="CancelOrderButton" TargetType="Button">
             <Setter Property="Background" Value="Transparent"/>
             <Setter Property="Foreground" Value="#64748B"/>
             <Setter Property="BorderThickness" Value="0"/>
             <Setter Property="Height" Value="45"/>
             <Setter Property="FontWeight" Value="SemiBold"/>
             <Setter Property="Cursor" Value="Hand"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="Button">
                         <Border Name="Border" CornerRadius="12" Background="{TemplateBinding Background}">
                             <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                         </Border>
                         <ControlTemplate.Triggers>
                             <Trigger Property="IsMouseOver" Value="True">
                                 <Setter Property="Foreground" Value="#DC2626"/>
                                 <Setter TargetName="Border" Property="Background" Value="#FEF2F2"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
         </Style>

         <!-- Checkout Button Style -->
         <Style x:Key="CheckoutButton" TargetType="Button">
             <Setter Property="Background" Value="#00288E"/>
             <Setter Property="Foreground" Value="White"/>
             <Setter Property="FontWeight" Value="Bold"/>
             <Setter Property="BorderThickness" Value="0"/>
             <Setter Property="Height" Value="45"/>
             <Setter Property="Cursor" Value="Hand"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="Button">
                         <Border Name="Border" CornerRadius="12" Background="{TemplateBinding Background}">
                             <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                         </Border>
                         <ControlTemplate.Triggers>
                             <Trigger Property="IsMouseOver" Value="True">
                                 <Setter TargetName="Border" Property="Background" Value="#1E40AF"/>
                             </Trigger>
                             <Trigger Property="IsEnabled" Value="False">
                                 <Setter TargetName="Border" Property="Background" Value="#E2E8F0"/>
                                 <Setter Property="Foreground" Value="#94A3B8"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
         </Style>
     </UserControl.Resources>

     <Grid Background="#F8FAFC">
         <Grid.ColumnDefinitions>
             <!-- Sidebar Column -->
             <ColumnDefinition Width="220"/>
             <!-- Main Content Column -->
             <ColumnDefinition Width="*"/>
         </Grid.ColumnDefinitions>

         <!-- 1. LEFT SIDEBAR -->
         <Border Grid.Column="0" Background="#1A1B22" BorderThickness="0">
             <Grid Margin="0,24,0,24">
                 <Grid.RowDefinitions>
                     <RowDefinition Height="Auto"/> <!-- Title/Terminal -->
                     <RowDefinition Height="*"/>    <!-- Navigation items -->
                     <RowDefinition Height="Auto"/> <!-- New Transaction Button -->
                 </Grid.RowDefinitions>

                 <!-- Brand Header -->
                 <StackPanel Grid.Row="0" Margin="20,0,20,32">
                     <TextBlock Text="SmartPOS Admin" Foreground="#E2E8F0" FontSize="18" FontWeight="Black"/>
                     <TextBlock Text="Terminal #01" Foreground="#94A3B8" FontSize="12" Margin="0,4,0,0" Opacity="0.8"/>
                 </StackPanel>

                 <!-- Navigation Links -->
                 <StackPanel Grid.Row="1">
                     <!-- Point of Sale (Active button) -->
                     <Border Background="#00288E" CornerRadius="8" Margin="8,2" Padding="16,10">
                         <StackPanel Orientation="Horizontal">
                             <TextBlock Text="🖥️" Margin="0,0,12,0" FontSize="14" VerticalAlignment="Center"/>
                             <TextBlock Text="Point of Sale" Foreground="White" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                         </StackPanel>
                     </Border>
                     <!-- Inactive buttons -->
                     <Button Style="{StaticResource SidebarInactiveButton}" Tag="🧾" Content="Transactions"/>
                     <Button Style="{StaticResource SidebarInactiveButton}" Tag="📦" Content="Inventory"/>
                     <Button Style="{StaticResource SidebarInactiveButton}" Tag="👤" Content="Customers"/>
                     <Button Style="{StaticResource SidebarInactiveButton}" Tag="🔄" Content="Returns &amp; Refunds"/>
                     <Button Style="{StaticResource SidebarInactiveButton}" Tag="🗂️" Content="Product Catalog"/>
                     <!-- Shift Reports link (navigates back to shift screen) -->
                     <Button Style="{StaticResource SidebarInactiveButton}" Tag="📊" Content="Shift Reports" Command="{Binding NavigateToShiftCommand}"/>
                     <Button Style="{StaticResource SidebarInactiveButton}" Tag="⚙️" Content="Settings"/>
                 </StackPanel>

                 <!-- New Transaction Button -->
                 <Button Grid.Row="2" Style="{StaticResource NewTransactionButton}" Content="+ New Transaction" Margin="12,0"/>
             </Grid>
         </Border>

         <!-- 2. MAIN CONTENT -->
         <Grid Grid.Column="1">
             <Grid.RowDefinitions>
                 <RowDefinition Height="64"/> <!-- Top Header -->
                 <RowDefinition Height="Auto"/> <!-- Error Messages -->
                 <RowDefinition Height="*"/>  <!-- Workspace -->
             </Grid.RowDefinitions>

             <!-- Top Header Bar -->
             <Border Grid.Row="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="20,0">
                 <Grid VerticalAlignment="Center">
                     <Grid.ColumnDefinitions>
                         <ColumnDefinition Width="*"/>
                         <ColumnDefinition Width="Auto"/>
                     </Grid.ColumnDefinitions>

                     <!-- Left: App Title and Info -->
                     <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                         <TextBlock Text="SMART STORE" Foreground="#00288E" FontSize="20" FontWeight="Black" VerticalAlignment="Center" Margin="0,0,24,0"/>
                         <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                             <!-- Shift info -->
                             <StackPanel Margin="0,0,16,0">
                                 <TextBlock Text="SHIFT INFO" Foreground="#64748B" FontSize="10" FontWeight="Bold"/>
                                 <TextBlock Text="Shift #0042" Foreground="#0F172A" FontSize="13" FontWeight="SemiBold" Margin="0,2,0,0"/>
                             </StackPanel>
                             <Border Width="1" Height="24" Background="#E2E8F0" Margin="0,0,16,0"/>
                             <!-- Staff info -->
                             <StackPanel Margin="0,0,16,0">
                                 <TextBlock Text="STAFF" Foreground="#64748B" FontSize="10" FontWeight="Bold"/>
                                 <TextBlock Text="Nguyen Van A" Foreground="#0F172A" FontSize="13" FontWeight="SemiBold" Margin="0,2,0,0"/>
                             </StackPanel>
                             <Border Width="1" Height="24" Background="#E2E8F0" Margin="0,0,16,0"/>
                             <!-- Current Time -->
                             <StackPanel>
                                 <TextBlock Text="CURRENT TIME" Foreground="#64748B" FontSize="10" FontWeight="Bold"/>
                                 <TextBlock Text="Jun 7 - 2026, 12:22 AM" Foreground="#0F172A" FontSize="13" FontFamily="Segoe UI" Margin="0,2,0,0"/>
                             </StackPanel>
                         </StackPanel>
                     </StackPanel>

                     <!-- Right: End Shift button -->
                     <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                         <Button Content="Support" Background="Transparent" Foreground="#00288E" FontWeight="Bold" BorderThickness="0" Margin="0,0,16,0" Cursor="Hand"/>
                         <Button Content="End Shift" Command="{Binding NavigateToShiftCommand}" Style="{StaticResource EndShiftButton}"/>
                     </StackPanel>
                 </Grid>
             </Border>

             <!-- Notification / Error Messages -->
             <Grid Grid.Row="1" Margin="20,12,20,0" Visibility="{Binding ErrorMessage, Converter={StaticResource NullOrEmptyToVisibilityConverter}, FallbackValue=Collapsed}">
                 <Border Background="#FEF2F2" BorderBrush="#FCA5A5" BorderThickness="1" CornerRadius="8" Padding="16,12">
                     <Grid>
                         <Grid.ColumnDefinitions>
                             <ColumnDefinition Width="*"/>
                             <ColumnDefinition Width="Auto"/>
                         </Grid.ColumnDefinitions>
                         <TextBlock Text="{Binding ErrorMessage}" Foreground="#991B1B" FontSize="13" FontWeight="Medium" VerticalAlignment="Center" TextWrapping="Wrap"/>
                         <Button Grid.Column="1" Content="✕" Foreground="#EF4444" Background="Transparent" BorderThickness="0" Command="{Binding ClearErrorMessageCommand}" VerticalAlignment="Center" Margin="12,0,0,0" Cursor="Hand" FontWeight="Bold"/>
                     </Grid>
                 </Border>
             </Grid>

             <!-- Workspace Area -->
             <Grid Grid.Row="2">
                 <Grid.ColumnDefinitions>
                     <ColumnDefinition Width="*"/> <!-- Product Catalog -->
                     <ColumnDefinition Width="420"/> <!-- Cart / Sidebar -->
                 </Grid.ColumnDefinitions>

                 <!-- LEFT: Products Selection -->
                 <Grid Grid.Column="0" Margin="20">
                     <Grid.RowDefinitions>
                         <RowDefinition Height="Auto"/> <!-- Barcode & Search Bar -->
                         <RowDefinition Height="Auto"/> <!-- Category Filters -->
                         <RowDefinition Height="*"/>    <!-- Products Grid -->
                     </Grid.RowDefinitions>

                     <!-- Barcode & Search Bar row -->
                     <Grid Grid.Row="0" Margin="0,0,0,12">
                         <Grid.ColumnDefinitions>
                             <ColumnDefinition Width="*"/>
                             <ColumnDefinition Width="Auto"/>
                         </Grid.ColumnDefinitions>

                         <!-- Search/Barcode box -->
                         <Border Grid.Column="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="12,4">
                             <Grid>
                                 <Grid.ColumnDefinitions>
                                     <ColumnDefinition Width="Auto"/>
                                     <ColumnDefinition Width="*"/>
                                 </Grid.ColumnDefinitions>
                                 <TextBlock Text="🔍" FontSize="16" Foreground="#64748B" VerticalAlignment="Center" Margin="4,0,8,0"/>
                                 <TextBox Grid.Column="1" 
                                          Background="Transparent" 
                                          BorderThickness="0" 
                                          Foreground="#0F172A" 
                                          FontSize="14" 
                                          VerticalAlignment="Center"
                                          Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                                          Padding="0,8"
                                          x:Name="SearchBox">
                                     <TextBox.InputBindings>
                                         <KeyBinding Key="Enter" Command="{Binding SearchProductsCommand}"/>
                                     </TextBox.InputBindings>
                                 </TextBox>
                                 <!-- Placeholder -->
                                 <TextBlock Grid.Column="1" 
                                            Text="Scan barcode or search product name..." 
                                            Foreground="#94A3B8" 
                                            FontSize="14" 
                                            VerticalAlignment="Center"
                                            IsHitTestVisible="False" 
                                            Margin="2,0,0,0">
                                     <TextBlock.Style>
                                         <Style TargetType="TextBlock">
                                             <Setter Property="Visibility" Value="Collapsed"/>
                                             <Style.Triggers>
                                                 <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                                     <Setter Property="Visibility" Value="Visible"/>
                                                 </DataTrigger>
                                             </Style.Triggers>
                                         </Style>
                                     </TextBlock.Style>
                                 </TextBlock>
                             </Grid>
                         </Border>

                         <!-- Search Button -->
                         <Button Grid.Column="1" 
                                 Content="Tìm kiếm" 
                                 Command="{Binding SearchProductsCommand}" 
                                 Style="{StaticResource SearchButton}" 
                                 Margin="8,0,0,0" 
                                 Width="100"/>
                     </Grid>

                     <!-- Category Filters Row -->
                     <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="0,0,0,16">
                         <StackPanel Orientation="Horizontal">
                             <RadioButton Content="All" IsChecked="True" Style="{StaticResource CategoryFilterButton}"/>
                             <RadioButton Content="Food &amp; Beverage" Style="{StaticResource CategoryFilterButton}"/>
                             <RadioButton Content="Electronics" Style="{StaticResource CategoryFilterButton}"/>
                             <RadioButton Content="Household" Style="{StaticResource CategoryFilterButton}"/>
                             <RadioButton Content="Personal Care" Style="{StaticResource CategoryFilterButton}"/>
                         </StackPanel>
                     </ScrollViewer>

                     <!-- Products Grid -->
                     <Grid Grid.Row="2">
                         <!-- Loader overlay -->
                         <ProgressBar IsIndeterminate="True" Height="3" VerticalAlignment="Top" Background="Transparent" Foreground="#00288E" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}" Panel.ZIndex="10"/>

                         <ScrollViewer Style="{StaticResource ModernScrollViewer}">
                             <ItemsControl ItemsSource="{Binding SearchResults}">
                                 <ItemsControl.ItemsPanel>
                                     <ItemsPanelTemplate>
                                         <WrapPanel Orientation="Horizontal" Margin="-6"/>
                                     </ItemsPanelTemplate>
                                 </ItemsControl.ItemsPanel>
                                 <ItemsControl.ItemTemplate>
                                     <DataTemplate>
                                         <Border Width="220" Height="265" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Margin="6" Padding="12">
                                             <Grid>
                                                 <Grid.RowDefinitions>
                                                     <RowDefinition Height="130"/> <!-- Image area -->
                                                     <RowDefinition Height="Auto"/> <!-- Title -->
                                                     <RowDefinition Height="Auto"/> <!-- SKU -->
                                                     <RowDefinition Height="*"/>    <!-- Price & Add -->
                                                 </Grid.RowDefinitions>

                                                 <!-- Image with overlay Badge -->
                                                 <Grid Grid.Row="0">
                                                     <Border CornerRadius="8" Background="#F1F5F9" ClipToBounds="True">
                                                         <Image Source="{Binding Sku, Converter={StaticResource ProductImageConverter}}" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                     </Border>
                                                     <!-- IN STOCK Badge -->
                                                     <Border HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8" Background="#ECFDF5" CornerRadius="8" Padding="6,3">
                                                         <StackPanel Orientation="Horizontal">
                                                             <Ellipse Width="6" Height="6" Fill="#16A34A" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                                             <TextBlock Text="IN STOCK" FontSize="9" FontWeight="Bold" Foreground="#16A34A" VerticalAlignment="Center"/>
                                                         </StackPanel>
                                                     </Border>
                                                 </Grid>

                                                 <!-- Product Title -->
                                                 <TextBlock Grid.Row="1" Text="{Binding Name}" FontWeight="Bold" FontSize="13" Foreground="#0F172A" Margin="0,8,0,0" TextTrimming="CharacterEllipsis"/>

                                                 <!-- SKU code -->
                                                 <TextBlock Grid.Row="2" Text="{Binding Sku, StringFormat='SKU: {0}'}" FontSize="11" Foreground="#64748B" Margin="0,2,0,0" FontFamily="Consolas"/>

                                                 <!-- Price and Add button -->
                                                 <Grid Grid.Row="3" VerticalAlignment="Bottom" Margin="0,0,0,2">
                                                     <Grid.ColumnDefinitions>
                                                         <ColumnDefinition Width="*"/>
                                                         <ColumnDefinition Width="Auto"/>
                                                     </Grid.ColumnDefinitions>
                                                     <!-- Price -->
                                                     <TextBlock Grid.Column="0" Text="{Binding UnitPrice, StringFormat='{}{0:N0} ₫'}" FontSize="16" FontWeight="Black" Foreground="#00288E" VerticalAlignment="Center"/>
                                                     <!-- Add Button -->
                                                     <Button Grid.Column="1" 
                                                             Content="+" 
                                                             Style="{StaticResource AddProductButton}" 
                                                             Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                             CommandParameter="{Binding}"/>
                                                 </Grid>
                                             </Grid>
                                         </Border>
                                     </DataTemplate>
                                 </ItemsControl.ItemTemplate>
                             </ItemsControl>
                         </ScrollViewer>
                     </Grid>
                 </Grid>

                 <!-- RIGHT: Cart Sidebar -->
                 <Border Grid.Column="1" Background="White" BorderBrush="#E2E8F0" BorderThickness="1,0,0,0">
                     <Grid>
                         <Grid.RowDefinitions>
                             <RowDefinition Height="Auto"/> <!-- Header (Current Order) -->
                             <RowDefinition Height="*"/>    <!-- Cart Items ScrollView -->
                             <RowDefinition Height="Auto"/> <!-- Footer Calculations & Actions -->
                         </Grid.RowDefinitions>

                         <!-- Cart Header -->
                         <Border Grid.Row="0" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="20,16">
                             <Grid>
                                 <StackPanel>
                                     <TextBlock Text="Current Order" FontSize="15" FontWeight="Bold" Foreground="#0F172A"/>
                                     <TextBlock Text="#ORD-20260606-0089" FontSize="12" Foreground="#64748B" Margin="0,2,0,0" FontFamily="Consolas"/>
                                 </StackPanel>
                                 <!-- Clear all -->
                                 <Button HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Command="{Binding ClearCartCommand}" Padding="4" ToolTip="Xóa tất cả">
                                     <TextBlock Text="🗑️" FontSize="18" Foreground="#64748B"/>
                                 </Button>
                             </Grid>
                         </Border>

                         <!-- Cart Items List -->
                         <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                             <ItemsControl ItemsSource="{Binding Cart.Items}">
                                 <ItemsControl.ItemTemplate>
                                     <DataTemplate>
                                         <Border BorderBrush="#F1F5F9" BorderThickness="0,0,0,1" Padding="16,12">
                                             <Grid>
                                                 <Grid.ColumnDefinitions>
                                                     <ColumnDefinition Width="Auto"/> <!-- Thumbnail image -->
                                                     <ColumnDefinition Width="*"/>    <!-- Name / Unit Price -->
                                                     <ColumnDefinition Width="Auto"/> <!-- Qty adjuster -->
                                                     <ColumnDefinition Width="80"/>   <!-- Subtotal -->
                                                     <ColumnDefinition Width="Auto"/> <!-- Delete button -->
                                                 </Grid.ColumnDefinitions>

                                                 <!-- Thumbnail image -->
                                                 <Border Grid.Column="0" Width="44" Height="44" CornerRadius="6" Background="#F1F5F9" ClipToBounds="True" Margin="0,0,10,0">
                                                     <Image Source="{Binding ProductSku, Converter={StaticResource ProductImageConverter}, FallbackValue='https://images.unsplash.com/photo-1546069901-ba9599a7e63c?w=100'}" Stretch="UniformToFill"/>
                                                 </Border>

                                                 <!-- Product details -->
                                                 <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                     <TextBlock Text="{Binding ProductName}" FontWeight="Bold" FontSize="13" Foreground="#0F172A" TextTrimming="CharacterEllipsis"/>
                                                     <TextBlock Text="{Binding UnitPrice, StringFormat='{}{0:N0} ₫'}" FontSize="11" Foreground="#64748B" Margin="0,2,0,0"/>
                                                 </StackPanel>

                                                 <!-- Quantity adjustments -->
                                                 <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                                                     <Button Content="-" 
                                                             Style="{StaticResource QtyAdjustButton}"
                                                             Command="{Binding DataContext.DecreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                             CommandParameter="{Binding}"/>
                                                     <TextBlock Text="{Binding Quantity}" 
                                                                VerticalAlignment="Center" 
                                                                HorizontalAlignment="Center" 
                                                                Width="28" 
                                                                TextAlignment="Center" 
                                                                FontWeight="Bold" 
                                                                Foreground="#0F172A"
                                                                FontSize="12"/>
                                                     <Button Content="+" 
                                                             Style="{StaticResource QtyAdjustButton}"
                                                             Command="{Binding DataContext.IncreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                             CommandParameter="{Binding}"/>
                                                 </StackPanel>

                                                 <!-- Subtotal -->
                                                 <TextBlock Grid.Column="3" 
                                                            Text="{Binding Subtotal, StringFormat='{}{0:N0} ₫'}" 
                                                            FontWeight="Bold" 
                                                            Foreground="#0F172A" 
                                                            FontSize="13" 
                                                            HorizontalAlignment="Right" 
                                                            VerticalAlignment="Center"/>

                                                 <!-- Remove item button -->
                                                 <Button Grid.Column="4" 
                                                         Content="✕" 
                                                         Style="{StaticResource RemoveCartItemButton}"
                                                         Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                         CommandParameter="{Binding}"/>
                                             </Grid>
                                         </Border>
                                     </DataTemplate>
                                 </ItemsControl.ItemTemplate>
                             </ItemsControl>
                         </ScrollViewer>

                         <!-- Cart Footer calculations & checkout -->
                         <Border Grid.Row="2" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" Background="#FFFFFF" Padding="20,16">
                             <StackPanel>
                                 <!-- Customer Info Row -->
                                 <Border Background="#F8FAFC" CornerRadius="8" Padding="12,8" Margin="0,0,0,12">
                                     <Grid>
                                         <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                             <TextBlock Text="👤" FontSize="14" Margin="0,0,8,0"/>
                                             <TextBlock Text="{Binding CustomerInfo}" FontWeight="Medium" Foreground="#0F172A" FontSize="12"/>
                                         </StackPanel>
                                         <Button Content="Add Member" Background="Transparent" Foreground="#00288E" FontWeight="Bold" FontSize="12" BorderThickness="0" HorizontalAlignment="Right" Cursor="Hand"/>
                                     </Grid>
                                 </Border>

                                 <!-- Promo Code input -->
                                 <Grid Margin="0,0,0,16">
                                     <Grid.ColumnDefinitions>
                                         <ColumnDefinition Width="*"/>
                                         <ColumnDefinition Width="Auto"/>
                                     </Grid.ColumnDefinitions>
                                     <TextBox Style="{StaticResource PromoTextBox}" 
                                              x:Name="PromoCodeInput" 
                                              Text="{Binding BarcodeQuery, UpdateSourceTrigger=PropertyChanged}">
                                         <TextBox.InputBindings>
                                             <KeyBinding Key="Enter" Command="{Binding ScanBarcodeCommand}"/>
                                         </TextBox.InputBindings>
                                     </TextBox>
                                     <!-- Placeholder -->
                                     <TextBlock Text="🏷️ Promo code" 
                                                Foreground="#94A3B8" 
                                                FontSize="13" 
                                                VerticalAlignment="Center" 
                                                IsHitTestVisible="False" 
                                                Margin="10,0,0,0">
                                         <TextBlock.Style>
                                             <Style TargetType="TextBlock">
                                                 <Setter Property="Visibility" Value="Collapsed"/>
                                                 <Style.Triggers>
                                                     <DataTrigger Binding="{Binding Text, ElementName=PromoCodeInput}" Value="">
                                                         <Setter Property="Visibility" Value="Visible"/>
                                                     </DataTrigger>
                                                 </Style.Triggers>
                                             </Style>
                                         </TextBlock.Style>
                                     </TextBlock>
                                     <Button Grid.Column="1" 
                                             Content="Apply" 
                                             Command="{Binding ScanBarcodeCommand}" 
                                             Style="{StaticResource ApplyPromoButton}" 
                                             Margin="8,0,0,0"/>
                                 </Grid>

                                 <!-- Calculations list -->
                                 <StackPanel Margin="0,0,0,16">
                                     <!-- Subtotal -->
                                     <Grid Margin="0,2">
                                         <TextBlock Text="Subtotal" Foreground="#64748B" FontSize="13"/>
                                         <TextBlock Text="{Binding Cart.Subtotal, StringFormat='{}{0:N0} ₫'}" Foreground="#64748B" FontSize="13" HorizontalAlignment="Right"/>
                                     </Grid>
                                     <!-- Discount -->
                                     <Grid Margin="0,2" Visibility="{Binding Cart.DiscountAmount, Converter={StaticResource DecimalToVisibilityConverter}, FallbackValue=Collapsed}">
                                         <TextBlock Text="Discount" Foreground="#64748B" FontSize="13"/>
                                         <TextBlock Text="{Binding Cart.DiscountAmount, StringFormat='-{0:N0} ₫'}" Foreground="#DC2626" FontSize="13" HorizontalAlignment="Right"/>
                                     </Grid>
                                     <!-- Tax -->
                                     <Grid Margin="0,2">
                                         <TextBlock Text="Tax (10%)" Foreground="#64748B" FontSize="13"/>
                                         <TextBlock Text="{Binding Cart.TaxAmount, StringFormat='{}{0:N0} ₫'}" Foreground="#64748B" FontSize="13" HorizontalAlignment="Right"/>
                                     </Grid>
                                     <!-- Total -->
                                     <Border Height="1" Background="#E2E8F0" Margin="0,8"/>
                                     <Grid>
                                         <TextBlock Text="Total" FontSize="18" FontWeight="Bold" Foreground="#0F172A" VerticalAlignment="Center"/>
                                         <TextBlock Text="{Binding Cart.TotalAmount, StringFormat='{}{0:N0} ₫'}" FontSize="24" FontWeight="Black" Foreground="#00288E" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                     </Grid>
                                 </StackPanel>

                                 <!-- Actions -->
                                 <Grid>
                                     <Grid.ColumnDefinitions>
                                         <ColumnDefinition Width="*"/>
                                         <ColumnDefinition Width="*"/>
                                     </Grid.ColumnDefinitions>
                                     <Button Grid.Column="0" 
                                             Content="Cancel Order" 
                                             Command="{Binding CancelOrderCommand}" 
                                             Style="{StaticResource CancelOrderButton}" 
                                             Margin="0,0,6,0"/>
                                     <Button Grid.Column="1" 
                                             Content="Checkout" 
                                             Command="{Binding CheckoutCommand}" 
                                             Style="{StaticResource CheckoutButton}" 
                                             Margin="6,0,0,0"/>
                                 </Grid>
                             </StackPanel>
                         </Border>
                     </Grid>
                 </Border>
             </Grid>
         </Grid>
     </Grid>
</UserControl>
