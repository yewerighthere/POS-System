<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Colors.xaml"/>
        <ResourceDictionary Source="Spacing.xaml"/>
        <ResourceDictionary Source="Shadows.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- Modal Styles — translated from style.css modal-overlay, modal classes -->

    <!-- Modal Overlay (.modal-overlay) -->
    <Style x:Key="ModalOverlayStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource OverlayBrush}"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
    </Style>

    <!-- Modal Dialog (.modal) -->
    <Style x:Key="ModalDialogStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="CornerRadius" Value="{StaticResource RadiusMedium}"/>
        <Setter Property="Effect" Value="{StaticResource ModalShadowEffect}"/>
        <Setter Property="Padding" Value="{StaticResource PaddingLarge}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="MinWidth" Value="400"/>
        <Setter Property="MaxWidth" Value="600"/>
    </Style>

</ResourceDictionary>
